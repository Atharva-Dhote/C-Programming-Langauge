#include <stdio.h>
int main() 
{
    int n,f;

        printf("\n  enter any number: ");
       scanf("%d",&n);
       f=fact(n);
        printf("\n factorial=%d",f);
}

int fact(int n)
{
  if(n==0)
  {
      return 1;
  }
else
{
    return n*fact(n-1);
}
}

