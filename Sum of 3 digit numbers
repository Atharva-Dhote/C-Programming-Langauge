#include <stdio.h>

int  main() {
   int d1,d2,d3,num,sum,ori;
    printf("Enter any three values:");
    scanf("%d",&num);
    ori=num;
    d1=num%10;
    num=num/10;
    d2=num%10;
    num=num/10;
    d3=num%10;
    sum=d1+d2+d3;
    printf("\n sum of digit of number %d is %d",ori,sum);

}
